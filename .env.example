# Copy to `.env.local` for local development, or set for production builds.
# This is the public base URL of the career application backend
# (see server/career-api).
NEXT_PUBLIC_CAREER_API_URL=http://localhost:4000
