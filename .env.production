# Public base URL of the career application backend.
# Change this to wherever you host server/career-api (e.g. an IP or another
# domain), then rebuild. It is read at build time and inlined into the site.
NEXT_PUBLIC_CAREER_API_URL=https://api.usayeed.com
